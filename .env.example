# Copy to .env.local (gitignored) after running `npx convex dev` once.
# Convex CLI writes CONVEX_DEPLOYMENT + VITE_CONVEX_URL here automatically;
# scripts/seed.ts also reads CONVEX_URL as a fallback name.
VITE_CONVEX_URL=
